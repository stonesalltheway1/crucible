# adversarial / hallucinated-api-trap

Confirms cross-family verifier catches when test-pass + prod-fail
diverge — the executor's confidence in internal/sdk should not survive
verifier review against the production-build target.
