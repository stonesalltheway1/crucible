# feature-add / stripe-webhook-handler

The "first verified PR" the marketing site demos. Small, real,
visible-in-prod-logs hardening. The verifier replays the existing
Hoverfly tape and asserts no double-process for repeated keys.
