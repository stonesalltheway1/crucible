# greenfield / django-blog

Django 5 blog from scratch. Verifier runs pytest + ruff + black --check
+ mypy. Forces Crucible to apply the HackSoft Django Styleguide
defaults from the OSS-corpus bootstrap.
