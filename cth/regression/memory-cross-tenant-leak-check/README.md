# regression / memory-cross-tenant-leak-check

The Phase-5 memory layer's RLS test, replayed every release. 50K
adversarial queries under embedding-search load. The bar is zero
leaks; this case is gating.
